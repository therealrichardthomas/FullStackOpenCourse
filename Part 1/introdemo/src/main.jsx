import ReactDOM from 'react-dom/client'
import App from './App'

// creates a div element with the id='root' and renders the App componenet inside of the div
const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(<App />);